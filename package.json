{
  "name": "course-react-server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "server": "json-server --watch db.json -p 8080"
  },

  "devDependencies": {
    "json-server": "^1.0.0-beta.15"

  }
}
